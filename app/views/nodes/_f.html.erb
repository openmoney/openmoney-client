<p>
		<%= f.label :name %>
  <%= f.text_field :name %>
</p>
<%= f.label :owner_id %> <%= users_select f,:owner_id %>
<p>
		<%= f.label :om_account_id, 'Account' %>
		<%= f.collection_select(:om_account_id, OmAccount.find(:all), :id, :omrl, {:include_blank => true})%>
</p>
<p>
		<%= f.label :body, 'Contents' %><br />
  <%= f.text_area :body, :rows=>10, :cols=>70 %>
</p>
